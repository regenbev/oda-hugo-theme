extends partials/base.amber

block content
  section
    h4 Latest posts
    ul.demo-list-three.mdl-list
    each $i, $page in where(Site.Pages, "Kind", "page")
      if $i < Site.Params.latestPostsCount
        import partials/li.amber
